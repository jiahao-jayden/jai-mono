# Directory Structure

> Frontend directory structure for `@jayden/jai-utils`.

---

This is a backend utility package. No UI components. The types and functions exported by this package (e.g., `NamedError`, `ParsedModelId`, `parseModelId`) can be imported by frontend code, but the package itself contains no frontend-specific code, components, or assets.
