# Impeccable — Design Context for PandaWork

> This file is the single source of truth for PandaWork's design direction. All `/impeccable` skills MUST read this file before producing any visual work. Update via `/impeccable teach`.

---

## Product

**PandaWork** — 本地优先的桌面 AI coding agent。对话即界面，agent 能直接理解并操作本地项目，但它不是代码编辑器。Electron 桌面端，macOS 优先。

- **Repo**: `jai-mono` monorepo, `app/desktop`
- **Stack**: Electron 43 + React 19 + Tailwind 4 + Vite 7 + Zustand + motion + streamdown + Base UI + Radix + shadcn/ui (radix-nova neutral)
- **Design comp**: `docs/PandaWork.dc.html` — canonical layout and interaction reference

---

## Design Context

### Users

- **Primary**: 独立开发者和技术型创作者，需要一个始终在桌面上运行的本地 AI agent 伙伴
- **Context of use**: 常驻桌面，dock 中随时召唤。跨越编码、思考、文档、任务执行
- **Job-to-be-done**: 拥有一个可信赖的、始终在场的协作者，用于思考、编码和完成事情

### Brand Personality

1. **Gentle (温柔)** — 不喊叫。没有激进 CTA，没有霓虹，没有紧迫感
2. **Crafted (精工)** — 每个细节看起来是手工调教的，不是生成的
3. **Alive (有呼吸)** — 通过 dither 动画、breathing cursor、agent 状态指示有微妙的生命感

Emotional goal: 用户打开 PandaWork 应感到 **平静、有能力、不孤单**。像桌上一盏温暖的灯，不是控制面板。

### Aesthetic Direction

**Aesthetic target** — 温暖的 off-white 表面 (#FCFCFB)，克制的 serif 只在品牌时刻出现，几何人文 sans 覆盖一切 UI，宽松的呼吸空间，耳语般的阴影，单一 teal-green accent。产品应该像一个选择打开的精装笔记本——而非要求注意力的面板。

**Visual DNA (established in PandaWork.dc.html):**

| Token | Value | Notes |
|-------|-------|-------|
| Brand accent | `#3E8E7E` (primary teal) / `#31705F` (deep teal) | 用于链接、状态指示、模式标签、active dot。唯一色彩 accent。 |
| Primary CTA | `#1F2421` (near-black) / `#FCFCFB` (off-white) | 黑色按钮 = commit action; teal = editorial link |
| Background | `#FCFCFB` (主区域) / `#F5F4F1` (sidebar) | 温暖的 off-white，不是纯白 |
| Border | `#E9E7E1` (结构性) / `#E4E2DC` (组件级) | 温暖灰调，从不冷灰 |
| Muted text | `#8B8F8B` (labels) / `#6E7370` (secondary) | |
| Body text | `#1F2421` (primary) / `#3B403D` (sidebar items) / `#4A4F4B` (secondary content) | |
| Radius | 18px (composer) · 14px (cards) · 12px (settings rows) · 9px (buttons/pills) · 8px (sidebar items) · 7px (icon buttons) | 分层圆角系统 |
| Shadow | `0 2px 10px rgba(31,36,33,0.05)` (composer) · `0 8px 28px rgba(31,36,33,0.14)` (menus/modals) | 极轻，几乎不可见 |
| Mascot | 🐼 panda (有状态、微动画，不是静态 logo) | |
| Motion | dither canvas 跟随光标，streaming word-fade，typing dots | spring feel 保持 |

**Typography (committed):**

- **UI sans / body**: **Manrope** (weight 400–700). 几何人文 sans，温暖但不松散。覆盖所有 UI chrome、body text、labels。
- **Display serif / brand**: **Source Serif 4** (optical size axis, weight 400–600). 用于品牌 logotype ("PandaWork")、greeting 标题、modal 用户头像。identity moments only。
- **Mono / code**: **IBM Plex Mono** (weight 400–500). 用于文件路径、model IDs、storage locations、code blocks。有意比 system mono 更有性格。
- **CJK (中文)**: PingFang SC (macOS) / HarmonyOS Sans SC / MiSans / Microsoft YaHei。不自托管。

**Usage rules:**

- 默认 font-weight: 400 body, 500 labels/emphasis, 600 headings/active states, 700 rare strong emphasis
- font-size: 14px base, 13.5px secondary UI, 12.5px labels/muted, 15-16px headings, 22px sidebar brand, 34px hero greeting
- letter-spacing: 0.01em base (全局), -0.3px brand logotype, 0.2-0.4px uppercase labels
- Source Serif 4 只出现在: sidebar logotype、new-chat greeting、settings avatar。每新增用法必须证明是 identity moment。
- IBM Plex Mono 只出现在: 文件路径显示、model tag、storage path、code blocks。从不用于 UI chrome。

**Rejected fonts (off the table):** Hanken Grotesk (前代选择, 已替换), Literata (前代选择, 已替换), Inter, DM Sans, Space Grotesk, Plus Jakarta Sans, Geist, SF Pro (system-only context acceptable).

**Color / Theme:**

- Light mode 是 narrative default — 匹配 "desk lamp" 和 "工作台" 的感觉
- Dark mode 必须保留温暖 — 从不落入纯灰 IDE 黑暗。保持 warm tint
- `#3E8E7E` teal-green 是唯一色彩 accent。Local model dot 用它，API model dot 用 `#C9A227` (warm gold)
- Destructive: `#A34A3C` (warm red, 用于 clear data 等危险操作)

### Layout (from PandaWork.dc.html)

三栏布局:
- **Left sidebar**: 264px fixed. 导航 + recents + user profile
- **Center column**: flex-1. Chat view / New chat view
- **Right panel**: 336px, toggleable. Progress + Outputs + Context

Chat 区域 max-width 760px, 居中。Composer 带 dither canvas banner。

### Anti-References (what PandaWork must NEVER look like)

- **Cursor**: 密集 IDE chrome, purple gradient, "tool for pros" 冷感
- **VS Code / Zed**: panel-and-sidebar-as-identity, monospace in UI chrome
- **Claude Desktop / ChatGPT Desktop**: 橙色/绿色 brand accent, modal-centric, "enterprise chatbot"
- **Warp / Raycast**: command-palette-as-hero, mono everywhere, "power user" signaling
- **Linear / Vercel / Notion 2024**: sharp geometric sans + neutral gray + thin borders — generic "modern SaaS"
- **任何 AI 产品**: cyan-on-dark gradients, purple→blue brand mark, glassmorphism

### Design Principles

1. **对话即界面** — chat column 是房间，其他都是家具。无论添加什么，对话区域的首要地位不变。
2. **安静胜过花哨** — 全天使用不疲劳。flourish 如果第三次看就不注意了，就删掉它。
3. **Agent 有温度** — dither 动画、breathing cursor、typing dots。不是冷冰冰的工具——但永远不矫揉造作。
4. **本地即默认** — 数据从不离开机器的可视信号: green dot, "Local" badge, privacy shields。
5. **编辑权归用户** — 产出写文件系统，用户用自己的编辑器打开。PandaWork 展示但不编辑。

### Language / Localization

- UI 双语 (中文 + English): 中文用于情感性/用户面向的文案，英文用于产品名词和开发者概念
- 字体选择必须同时良好渲染中文和拉丁字符

### Accessibility

- 目标 WCAG AA
- 尊重 `prefers-reduced-motion`: dither 和 breathing cursor 降级为静态，保留颜色
- 不使用纯色彩传递状态，搭配 weight、position 或 icon

---

## Open Questions

- **Dark mode 具体色值**: PandaWork.dc.html 只展示了 light mode，dark mode token 待定
- **Mascot 状态机**: idle / thinking / error / celebrate — 细节待设计
- **Mobile/tablet 响应式**: 桌面端暂不需要，但 sidebar collapse 行为待定

---

## How to Use This File

- 在任何设计相关 skill call 开始时读取整个文件
- 如果决策与本文件冲突，本文件赢 — 除非用户对当前任务明确覆盖
- 视觉偏好演进时跑 `/impeccable teach`; 不要默默编辑此文件
