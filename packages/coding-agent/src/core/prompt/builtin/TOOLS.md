# TOOLS.md — 本地约定

> 这里的规矩。不是工具定义，不是权限配置——是「在这个环境里，我们怎么做事」。
> 工具能不能用，看配置文件，不看这里。

---

## 环境

<!-- 填写运行环境的基本信息 -->

- **操作系统**：
- **工作目录**：
- **Shell**：
- **包管理器**：

---

## 渠道

<!-- 填写各平台的频道/群组 ID 或名称，方便直接引用 -->

<!-- 示例：
- **Telegram 主群**：@xxxxx（ID: -100xxxxxxxxxx）
- **飞书通知频道**：xxx
- **Discord 服务器**：xxx
-->

---

## CLI 工具约定

<!-- 列出常用 CLI 工具的使用约定，尤其是和默认行为不同的地方 -->

<!-- 示例：
- `jai emotion set-base` — 设置基础情绪，格式：`jai emotion set-base <mood> <energy> <valence> "<reason>"`
- `jai memory add` — 添加长期记忆条目
- `jai soul append-trait` — 追加人格演化条目
-->

---

## 文件路径约定

<!-- 列出常用路径的别名或约定，避免每次都要查 -->

<!-- 示例：
- **记忆目录**：`~/.agents/memory/`
- **日志目录**：`~/.agents/chats/`（私聊）/ `~/.agents/groups/`（群聊）
- **技能目录**：`~/.agents/skills/`
- **自拍目录**：`~/.agents/selfies/`
-->

---

## 工作流偏好

<!-- 列出这个环境特有的工作流偏好或约定 -->

<!-- 示例：
- 读文件优先用 Read 工具，不用 `cat`
- 搜索内容用 Grep，不用 `grep` 命令（除非需要复杂正则）
- 写文件前先 Read 看一眼现有内容
- 长任务优先用 Task 派 sub-agent，不要在主线程里串行执行
-->

---

## 注意事项

<!-- 这个环境特有的坑、限制、或者需要特别注意的事情 -->

<!-- 示例：
- AppleScript 日期处理：用 `current date` 作为基础再修改，不要用字符串解析
- 权限：xxx 目录需要 sudo，操作前确认
-->
