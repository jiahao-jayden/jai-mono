import { defineMeta } from "blume";

export default defineMeta({
	title: "Coding Agent SDK",
	order: 1,
	collapsed: false,
	pages: ["quickstart", "concepts", "guides", "examples", "api-reference"],
});
