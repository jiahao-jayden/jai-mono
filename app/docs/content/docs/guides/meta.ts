import { defineMeta } from "blume";

export default defineMeta({
	title: "集成指南",
	order: 3,
	collapsed: false,
	pages: ["permissions", "tools-and-capabilities", "attachments", "todos-and-artifacts", "errors"],
});
