---
title: State 与 Events API
description: 查阅 CodingAgentState、CodingRunResult、CodingAgentEvent 与 JSON-safe message/工具事件载荷。
---

## `CodingAgentState<TAppState>`

```ts
interface CodingAgentState<TAppState extends JsonObject = JsonObject> {
  sessionId: string;
  status: "idle" | "running" | "aborted" | "closed";
  messages: readonly CodingAgentMessage[];
  todos: readonly CodingAgentTodo[];
  artifacts: readonly CodingAgentArtifact[];
  appState: TAppState;
  error?: JsonObject;
}
```

`CodingRunResult` 返回同一状态类型，并额外提供该次 run 的 `messages` 与 `sessionId`。

## `CodingAgentEvent`

事件 union 的关键载荷：

- Agent：`agent_start`；`agent_end { messages }`
- Turn：`turn_start`；`turn_end { message, toolResults }`
- Message：start/end 带 `message`；update 额外带 `assistantEvent`
- Tool：start 带 `toolCallId/toolName/title/args`；update 带 `partial`；end 带 `result/isError`
- Compaction：start 带 `trigger/tokensBefore`；end 带 `outcome`

`CodingAgentMessage` 是 SDK 自己定义的 JSON-safe user、assistant、toolResult union，不是 `@jai/ai` message 类型别名。assistant 内容可以是 text、thinking 或 tool call；tool result 内容可以是 text 或 image。

## JSON 类型

```ts
type JsonValue = null | boolean | number | string | JsonValue[] | JsonObject;
type JsonObject = { readonly [key: string]: JsonValue };
```

`state.error` 与工具事件载荷只承诺 JSON 边界，不承诺内部 TaggedError、Provider 或工具 result 的原型。
