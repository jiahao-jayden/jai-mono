import { defineMeta } from "blume";

export default defineMeta({
	title: "示例",
	order: 4,
	collapsed: true,
	pages: ["durable-session", "resume-session", "ephemeral-agent", "observe-events", "approval-flow"],
});
